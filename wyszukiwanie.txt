SELECT * FROM produkt AS p JOIN kategoria_2 AS k2 USING (kategoria_id) JOIN kategoria_1 AS k1 ON (k2.parent_id = k1.kategoria_id) JOIN kategoria AS k ON (k1.parent_id = k.kategoria_id) JOIN marka AS m USING (marka_id) WHERE (nazwa LIKE '%maska%') OR (opis LIKE '%maska%') OR (k2.kategoria LIKE '%maska%') OR (k1.kategoria LIKE '%maska%') OR (k.kategoria LIKE '%maska%') OR (marka LIKE '%maska%');